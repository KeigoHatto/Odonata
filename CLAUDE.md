# 作業ルール

Webサイトを編集する前に、必ず docs/ODONATA_WEB_GUIDELINES.md を読むこと。
特に §18（Coding Standards）と §19（Rules for Claude Code / AI）に従う。
作業完了時は §20 の QA Checklist で自己点検する。

## 応答が止まらないための制約（過去にAPIエラーで中断した経緯あり）

- 既存ファイルの全体書き直し（Write）は禁止。Edit の部分置換のみ
- 1回の応答で書き出すのは1ファイルの1セクションまで
- 1項目ずつ処理し、項目ごとにコミット。push はフェーズ完了時に1回
- 巨大ファイルを丸ごと読まない。必要な行範囲だけ読む
- 同じエラーが3回続いたら止めて報告する
